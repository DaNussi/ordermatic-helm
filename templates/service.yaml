apiVersion: v1
kind: Service
metadata:
  name: {{ include "ordermatic-helm.fullname" . }}
  labels:
    {{- include "ordermatic-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ordermatic-helm.selectorLabels" . | nindent 4 }}
